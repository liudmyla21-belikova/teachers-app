# 🍎 LearnLingo — Платформа для пошуку репетиторів

**LearnLingo** — сучасний веб-додаток для пошуку та бронювання викладачів іноземних мов. Користувачі можуть шукати вчителів за мовою, рівнем знань і ціною, додавати їх в обране та записуватися на пробні заняття.

## 📐 Макет та дизайн

Проєкт реалізовано відповідно до макету в Figma:

- **Колірна палітра:** пастельні тони (основний колір: `#E0A39A`)
- **Шрифти:** Roboto
- **Адаптивність:** інтерфейс коректно відображається на десктопах і мобільних пристроях

[Переглянути макет у Figma](https://www.figma.com/design/dewf5jVviSTuWMMyU3d8Mc/Learn-Lingo?node-id=0-1&p=f&t=3VTbPPxwUnT1oSsB-0)

## 🚀 Функціонал

- **Авторизація:** реєстрація та вхід через Firebase Auth
- **База даних:** зберігання даних про викладачів та відгуки у Firebase Realtime Database
- **Фільтрація:** пошук за мовою, рівнем (A1–C2) та ціною
- **Обране:** можливість зберігати улюблених викладачів (для авторизованих користувачів)
- **Пагінація:** поступове завантаження списку викладачів ("Load More")
- **Адаптивність:** підтримка мобільних пристроїв

## 📋 Технічне завдання

Під час розробки реалізовано:

1. **Сторінка вчителів:** список карток з пагінацією
2. **Фільтрація:** динамічний пошук за мовою, рівнем та вартістю за годину
3. **Авторизація:** реєстрація та вхід; дані користувачів зберігаються у Firebase
4. **Обране:** додавання викладачів до обраного з синхронізацією через Redux та localStorage
5. **Бронювання:** модальне вікно для запису на пробний урок з валідацією полів
6. **Безпека:** Private Routes — сторінка "Favorites" доступна лише авторизованим користувачам

## 🛠 Технології

- **React** (Vite)
- **Firebase** (Auth & Realtime Database)
- **React Router DOM** (навігація)
- **React Spinners** (індикація завантаження)
- **CSS Modules** (стилізація)

## 📦 Встановлення та запуск

1. Клонуйте репозиторій:

```bash
git clone https://github.com/liudmyla21-belikova/teachers-app.git
```

2. Перейдіть у папку проєкту:

```bash
cd teachers-app
```

3. Встановіть залежності:

```bash
npm install
```

4. Створіть файл `.env` у корені проєкту та додайте ключі Firebase:

```env
VITE_FIREBASE_API_KEY=your_key
VITE_FIREBASE_AUTH_DOMAIN=your_domain
VITE_FIREBASE_DATABASE_URL=your_url
VITE_FIREBASE_PROJECT_ID=your_id
VITE_FIREBASE_STORAGE_BUCKET=your_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

5. Запустіть проєкт у режимі розробки:

```bash
npm run dev
```
